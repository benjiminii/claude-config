# graphify
- **graphify** (`~/.claude/skills/graphify/SKILL.md`) - any input to knowledge graph. Trigger: `/graphify`
When the user types `/graphify`, invoke the Skill tool with `skill: "graphify"` before doing anything else.

# Git commits (global, required)
- **Never** add your own name / AI authorship to a commit. No `Co-Authored-By` trailer, no agent/AI attribution in the message or trailers.
- Always commit with **two `-m` flags**:
  - **1st `-m`**: conventional subject — `fix:` / `chore:` / `feat:` / `refactor:` etc. + short description.
  - **2nd `-m`**: a more in-depth description of the change — concise, not too long.
